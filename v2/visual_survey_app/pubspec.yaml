name: visual_survey_app
description: A new Flutter application.

publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ">=2.7.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
  animated_text_kit:
  firebase_core:
  cloud_firestore:
  firebase_auth:
  firebase_storage: 
  modal_progress_hud:
  animated_widgets: ^1.0.6
  intl: ^0.17.0
  flutter_svg_provider:
  email_validator: ^2.0.1
  change_app_package_name: ^1.0.0
  flutter_launcher_icons: ^0.9.0
  flutter_native_splash: ^1.1.8+4
  progress_indicators: ^0.1.5
  image_picker:
  flutter_spinkit: ^4.1.2+1
  date_format: ^1.0.9
  flutter_image_compress: 0.5.0

dev_dependencies:
  flutter_test:
    sdk: flutter

flutter:

  uses-material-design: true
  assets:
    - images/

flutter_native_splash:
  image: images/icon.png
  color: "212121"